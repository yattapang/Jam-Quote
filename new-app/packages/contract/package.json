{
  "name": "@pryvis/contract",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The wire contract web and mobile consume. Generated from the API's @wire types; never hand-edited (ADR 0012).",
  "main": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "contract:generate": "node --experimental-strip-types generate.ts"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}

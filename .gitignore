# Dependencies
node_modules/
.pnp.*
.yarn/*

# Builds
dist/
build/
.next/
out/
.turbo/
*.tsbuildinfo

# Expo
.expo/
web-build/
*.apk
*.aab
*.ipa

# Env & secrets
.env
.env.*
!.env.example

# Prisma
original-app/apps/api/prisma/*.db
original-app/apps/api/prisma/migrations/dev/

# Logs
*.log
npm-debug.log*

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# Claude Code local (personal) settings
**/.claude/settings.local.json
# Everything under .claude is local EXCEPT the review agent briefs, which are
# project infrastructure: nine section reviewers plus commit-reviewer, carrying the
# defect taxonomy and review method this codebase was hardened with. They were
# ignored, so a fresh clone got none of them. Same idiom as .vscode above — a
# directory-level ignore cannot be undone from inside, hence `/*` plus a negation.
**/.claude/
!/.claude/
/.claude/*
!/.claude/agents/

# Design source (kept out of app build, retained locally)
extracted/.thumbnail

# A review agent may leave probe files while it works. They are its scratch space,
# not project code, and must never reach main — my own `git add -A` committed one once.
new-app/**/zz-probe*.ts
new-app/**/zz-review-probe*.ts
